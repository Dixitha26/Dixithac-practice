Write a program to find which number is greatest among two numbers.

Input Format:

Input consists of two integers.
#include<iostream>
using namespace std;
int main()
{
    int a,b;
    cin>>a>>b;
    if(a>b)
    {
        cout<<a<<" is greater";
        
    }
    else
    cout<<b<<" is greater";
} 
